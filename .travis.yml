sudo: false
language: ruby
rvm:
  - 2.3.0
script:
  - RAILS_ENV=test bundle exec rake db:setup
  - bundle exec rake
notifications:
  email:
    - travisci@dennmart.com
env:
  - ECHO_TRELLO_AIRBRAKE_API="1234567890"
  - ECHO_TRELLO_AIRBRAKE_HOST="https://airbrake.com"
